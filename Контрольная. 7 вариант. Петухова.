Вариант 7. Петухова.
1. Создание таблицы.

CREATE TABLE Киносеанс (
    Сеанс_id VARCHAR(10) PRIMARY KEY,
    Фильм VARCHAR(100),
    Жанр VARCHAR(50),
    Продолжительность_мин INT,
    Зал INT,
    Вместительность_зала INT
);

//наполнение таблицы данными.

INSERT INTO киносеанс (`Сеанс_id`, Фильм, Жанр, Продолжительность, Зал, `Вместительность_зала`)
VALUES
('A101', 'Не смотрите наверх','Сатира', 138, 1, 150),
('A102', 'Довод','Боевик', 150, 2, 100),
('A103', 'Не смотрите наверх','Сатира', 138, 3, 80);

2. Транзитивная зависимость.

1. Атрибут Вместительность_зала зависит не от первичного ключа (Сеанс_id), а от неключевого атрибута "Зал". 
(Зал → Вместительность_зала)

3. 3НФ. 

CREATE TABLE Залы (
 Зал_id INT PRIMARY KEY,
    Номер_зала INT NOT NULL UNIQUE,
    Вместительность INT NOT NULL
);

CREATE TABLE Фильмы (
 Фильм_id INT PRIMARY KEY,
    Название_фильма VARCHAR(10) NOT NULL,
    Жанр VARCHAR(50),
    Продолжительность INT NOT NULL
);

CREATE TABLE Сеансы (
 Сеанс_id VARCHAR(10) PRIMARY KEY,
    Фильм_id INT NOT NULL,
    Зал_id INT NOT NULL,
    FOREIGN KEY (Фильм_id) REFERENCES Фильмы(Фильм_id),
    FOREIGN KEY (Зал_id) REFERENCES Залы(Зал_id)
);

Новые сущности и их атрибуты после приведения к 3НФ: 
Сущность: Залы.
Атрибуты: Зал_id (PRIMARY KEY), Номер_зала, Вместительность.

Сущность: Фильмы.
Атрибуты: Фильм_id (PRIMARY KEY), название_фильма, Жанр, Продолжительность.

Сущность: Сеансы.
Атрибуты: Сеанс_id (PRIMARY KEY), Фильм_id (FOREIGN KEY), Зал_id (FOREIGN KEY).


Связи:
Сеансы -> Фильмы
Сеансы -> Залы
